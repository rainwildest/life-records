declare type FundPlanOptions = {
  expenseId: string;
  userId: string;
  amounts: number;
  approximateAt: Date;
};

declare type FundPlanSnakeOptions = {
  expense_id: string;
  user_id: string;
  amounts: number;
  approximate_at: Date;
};
