# import CostDetailsOptions from '../fragments/cost-details.graphql'

query sameDay {
  sameDay {
    pay
    income
    details {
      ...CostDetailsOptions
      expense {
        id
        expenseName
        expenseType
      }
    }
  }
}
