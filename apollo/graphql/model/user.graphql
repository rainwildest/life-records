# import UserOptions from '../fragments/user.graphql'

query user {
  user {
    ...UserOptions
    statistics {
      pay {
        days
        months
        years
      }
      income {
        days
        months
        years
      }
    }
  }
}
