type FundPlan {
  id: ID!
  seqId: Int
  expenseId: String
  userId: String
  amounts: Int
  approximateAt: Date
  createdAt: Date
  modifiedAt: Date
  deletedAt: Date
  hasComplete: Boolean

  # custom data
  user: User
  expense: LivingExpenses
}

input fieldFundPlanOptions {
  amounts: Int
  expenseId: String
  approximateAt: Date
}

type Query {
  fundPlan(date: String): [FundPlan]
}

type Mutation {
  createFundPlan(input: fieldFundPlanOptions): FundPlan
  modifyFundPlan(id: String, input: fieldFundPlanOptions): FundPlan
  removeFundPlan(id: String!): FundPlan
}
