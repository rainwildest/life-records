type AccountBooks {
  id: ID!
  seqId: Int
  name: String
  userId: String
  createdAt: Date
  modifiedAt: Date
  deletedAt: Date
}

input fieldAccountBooks {
  name: String
}

type Mutation {
  createAccountBooks(name: String): AccountBooks
  modifyAccountBooks(id: String!, name: String): AccountBooks
  removeAccountBooks(id: String!): AccountBooks
}
